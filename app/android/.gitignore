gradle-wrapper.jar
/.gradle
# Kotlin build-session scratch (`.kotlin/sessions/*.salive`) — written by the
# Kotlin Gradle plugin during a build, never source.
/.kotlin/
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
